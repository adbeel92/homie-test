#!/bin/bash

if [ "$ANYCABLE_DEPLOYMENT" == "true" ]; then
  bundle exec anycable --server-command="anycable-go --headers=origin --log_level debug"
else
  bundle exec rails server -p $PORT -b 0.0.0.0
fi
